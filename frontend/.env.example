# Leave unset for local development so requests use /api through the Vite proxy.
# For a separately hosted backend, set the public backend origin.
# Both https://api.example.com and https://api.example.com/api are accepted.
VITE_API_URL=

# Optional: disable Vite HMR in constrained environments.
DISABLE_HMR=false
