# SPL Syntax Export Skill

This skill performs a deterministic code-based wrapping of RequirementSpace workbench model outputs into a valid SPL (Structured Prompt Language) DSL syntax file.
It does not invoke any LLM calls and is stable, repeatable, and fast.
